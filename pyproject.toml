[project]
name = "y402"
version = "0.1.0"
description = "Self-custodial CLI USDC wallet that speaks x402"
readme = "README.md"
license = "CC0-1.0"
license-files = ["LICENSE.md"]
requires-python = ">=3.14"
authors = [{ name = "Ivan Anishchuk", email = "ivan@ivananishchuk.net" }]
keywords = []
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.14",
    "Typing :: Typed",
]
dependencies = [
    "typer>=0.15",
    "rich>=13.0",
    "eth-account>=0.13.7",
    "web3>=7.16.0",
    "x402>=2.13.0",
    "keyring>=25.7.0",
    "qrcode>=8.2",
    "httpx>=0.28.1",
    "tomli-w>=1.2.0",
]

[project.urls]
Homepage = "https://github.com/IvanAnishchuk/y402"
Repository = "https://github.com/IvanAnishchuk/y402"
Issues = "https://github.com/IvanAnishchuk/y402/issues"
Changelog = "https://github.com/IvanAnishchuk/y402/blob/main/CHANGELOG.md"
Security = "https://github.com/IvanAnishchuk/y402/blob/main/SECURITY.md"

[project.scripts]
y402 = "y402.cli:app"

[dependency-groups]
dev = [
    "ruff>=0.11",
    "mypy>=1.13",
    "ty",
    "basedpyright>=1.21",
    "pytest>=8.0",
    "pytest-cov>=6.0",
    "pip-audit>=2.7",
    "pre-commit>=4.0",
    "zizmor>=1.0",
    "types-qrcode>=8.2.0.20260518",
]

[build-system]
# >=1.27 for stable PEP 639 license expression + license-files support.
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/y402"]

[tool.hatch.build.targets.sdist]
include = [
    "/src",
    "/tests",
    "/README.md",
    "/CHANGELOG.md",
    "/SECURITY.md",
    "/LICENSE.md",
    "/pyproject.toml",
]

[tool.ruff]
target-version = "py314"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E",     # pycodestyle errors
    "W",     # pycodestyle warnings
    "F",     # pyflakes
    "I",     # isort
    "N",     # pep8-naming
    "UP",    # pyupgrade
    "B",     # flake8-bugbear
    "S",     # flake8-bandit (security)
    "SIM",   # flake8-simplify
    "BLE",   # flake8-blind-except
    "TRY",   # tryceratops
    "TCH",   # flake8-type-checking
    "RUF",   # ruff-specific
    "PTH",   # flake8-use-pathlib
    "ERA",   # eradicate (commented-out code)
    "PIE",   # flake8-pie
    "T20",   # flake8-print
    "C4",    # flake8-comprehensions
    "PL",    # pylint
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = [
    "S101",    # assert is fine in tests
    "S603",    # subprocess with validated input in fixtures
    "S607",    # partial executable path in test setup
    "BLE001",  # broad Exception in test fixtures
    "T20",     # print is useful in test debugging
    "PLR2004", # magic values are idiomatic in test assertions
]

[tool.ruff.lint.pylint]
max-args = 8
max-returns = 8
max-branches = 15
max-statements = 60

[tool.ruff.lint.isort]
known-first-party = ["y402"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.mypy]
python_version = "3.14"
files = ["src", "tests"]
strict = true
warn_unused_configs = true
warn_redundant_casts = true
warn_unused_ignores = true

[tool.basedpyright]
pythonVersion = "3.14"
include = ["src", "tests"]
typeCheckingMode = "standard"
reportMissingTypeStubs = false

[tool.ty.environment]
python-version = "3.14"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = [
    "--strict-markers",
    "--strict-config",
    "--cov=y402",
    "--cov-config=pyproject.toml",
    "--cov-report=term-missing",
    "--cov-report=html:.reports/test/htmlcov",
    "--cov-report=xml:.reports/test/coverage.xml",
    "--junitxml=.reports/test/test-results.xml",
    "-ra",
]
junit_family = "xunit2"
junit_logging = "all"
junit_log_passing_tests = false

[tool.coverage.run]
source_pkgs = ["y402"]
branch = true
relative_files = true

[tool.coverage.report]
fail_under = 50
show_missing = true
exclude_lines = [
    "if __name__",
    "pragma: no cover",
]
